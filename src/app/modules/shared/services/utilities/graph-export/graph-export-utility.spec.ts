import { GraphExportUtility } from './graph-export-utility';

describe('GraphExportUtility', () => {
  it('should create an instance', () => {
    expect(new GraphExportUtility()).toBeTruthy();
  });
});
